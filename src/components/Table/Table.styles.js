import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  seat: {
    position: 'absolute',
    top: 40,
    left: 30
  },
  background: {
    resizeMode: 'contain',
    flex: 1
  }
});
