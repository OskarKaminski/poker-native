import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  card: {
    width: 40,
    height: 40,
    backgroundColor: 'white',
    alignItems: 'flex-end',
    borderWidth: 1
  },
  label: {
    fontSize: 20,
    padding: 5
  }
});
